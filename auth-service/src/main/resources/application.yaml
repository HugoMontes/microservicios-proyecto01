server:
  port: 8086

spring:
  application:
    name: auth-service
  config:
    import: optional:configserver:http://localhost:8085

jwt:
  secret: miClaveSuperSecretaParaJWT1234567

admin-paths: # Prefixo del @ConfigurationProperties en la clase RouteValidator
  paths:
    - uri: "/usuarios"   # URI que sera administrado por admin
      method: "POST"     # Metodo que sera administrado por admin